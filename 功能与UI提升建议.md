# 行迹 TRACE · 功能与 UI 提升建议

> 基于设计系统（`design.css` 宣纸+陶土橙令牌体系）和 14 个页面的扫描，按优先级排序。

---

## 一、高优先级（体验缺口大，投入产出比高）

### 1. 全局底部导航栏

**现状**：14 个页面靠页面内按钮互跳，没有统一导航。用户从「行程规划」想去「旅程回顾」得先回首页。

**建议**：加一个固定底部 Tab 栏，5 个入口：首页 / 地图 / 规划 / 回顾 / 我的。当前页高亮，过渡用 `--t-norm`。

**工作量**：中（抽一个 `nav.js` + `nav.css`，各页面引入）。

---

### 2. 行程规划：真实路径 & 距离

**现状**：`planner.js` 用 haversine 直线距离 + 贪心排期，实际驾驶时间偏差大（尤其川西/青藏这种山路）。

**建议**：已有高德 Key，调用「驾车路径规划 API」替换直线距离，排期时用真实耗时 + 途经点。结果页显示「总里程 / 总驾驶时间 / 每日驾驶时长」。

**收益**：规划结果从"看起来合理"变成"真能走"。

**工作量**：中（改 `planner.js` 的距离计算层，加缓存避免重复请求）。

---

### 3. 数据导出/备份闭环

**现状**：游记存 IndexedDB，行程/清单存 localStorage，**没有一键备份**。换设备或清缓存就全没了。

**建议**：设置页加「导出全部数据」（打包成 JSON + 图片 base64 的 ZIP，用已有 `vault.js` 的 ZIP 能力）和「导入恢复」。可选 WebDAV 同步（高级用户）。

**工作量**：中（ZIP 导出已有基础，主要是导入校验和冲突合并）。

---

### 4. 空状态统一设计

**现状**：`review.html` 有 empty 状态，但其他页面（`wishlist.html` 无收藏、`travel-map.html` 无游记、`planner.html` 无行程）大概率是空白或简陋提示。

**建议**：统一空状态组件：衬线大标题 + 一句引导文案 + 一个主操作按钮。比如「还没有游记，去记录第一段旅程」→ 按钮跳首页录音。

**工作量**：小（抽 `.empty-state` 组件，各页面替换）。

---

## 二、中优先级（功能深化，差异化竞争力）

### 5. 轨迹回放动画

**现状**：`travel-map.html` 显示游记落点，但没有动态感。

**建议**：加「播放旅程」按钮，按时间顺序在地图上画出行进轨迹（折线动画 + 移动标记点），同时侧边时间线高亮当前站。速度可调（1x/2x/5x）。

**技术**：Leaflet `polyline` + `requestAnimationFrame` 逐步增加点。

**工作量**：中。

---

### 6. 语音游记：分段录制 + 编辑后润色

**现状**：`travel-notes.js` 语音→转写→直接 AI 润色，中间不可干预。

**建议**：
- 支持「暂停/继续」分段录制，自动合并转写
- 转写结果先展示为可编辑文本，用户改完再点「AI 润色」
- 润色时保留用户修改过的段落（加标记，AI 不重写）

**收益**：语音转写常有错字，当前流程只能接受或重录。

**工作量**：中。

---

### 7. 基于游记的偏好分析 & 推荐

**现状**：有 7833 节点库和用户游记数据，但没有交叉利用。

**建议**：`review.html` 加一个「旅行画像」模块：
- 你最爱的主题类型（统计游记中景点的 theme 分布）
- 你去过的海拔区间
- 推荐 3 个「你可能喜欢但还没去」的景点（基于主题偏好 + 地理邻近）

**技术**：纯本地计算，不依赖 AI。

**工作量**：中。

---

### 8. 行程规划：住宿 & 预算

**现状**：排期只到景点，不管住哪、花多少钱。

**建议**：
- 每日行程末尾自动推荐「当晚住宿城市」（基于最后一个景点的最近城镇）
- 简单预算估算：门票（节点数据里有吗？没有的话用主题均值）+ 住宿（城市档位）+ 餐饮（按天）+ 油费（里程×油耗）

**工作量**：中（住宿推荐需要城市坐标库，`nation-index.js` 里可能已有城市级数据）。

---

## 三、UI/UX 细节提升

### 9. 页面转场动画

**现状**：多页面跳转是硬切，在 WebView 里体验割裂。

**建议**：统一转场：新页面从右侧滑入（`translateX(100%)→0` + `opacity`），返回时反向。用 `--t-norm`。PWA/Android WebView 下效果接近原生。

**工作量**：小（一个 `page-transition.css` + 监听 `popstate`）。

---

### 10. 骨架屏加载

**现状**：按省懒加载 `*-data.js` 时，地图区域可能空白等待。

**建议**：加载时显示骨架屏（灰色占位块 + 微光扫过动画），比空白或转圈更有品质感。

**工作量**：小。

---

### 11. 首页：最近游记 & 继续规划

**现状**：`index.html` 是杂志式入口，偏"展示"，缺少"继续上次操作"的快捷入口。

**建议**：Hero 下方加两个卡片：
- 「继续记录」— 显示最近一篇游记的缩略图 + 标题，点击进入编辑
- 「继续规划」— 如果有未完成的行程，显示行程名 + 进度，点击恢复

**工作量**：小（读 localStorage + IndexedDB 取最近一条）。

---

### 12. 深色模式全面审计

**现状**：`design.css` 有深色令牌，各页面有"深色补丁"注释，但 14 个页面的内联 CSS 里很可能有硬编码的 `#fff`、`rgba(255,255,255,...)` 残留。

**建议**：跑一遍 `tools/` 里已有的 `scan-dark-residue.js`（如果还能用），把所有硬编码颜色替换为 `var(--color-surface)` 等令牌。重点查弹窗、浮层、卡片背景。

**工作量**：中（机械替换，但要逐页验证）。

---

## 四、低优先级（锦上添花）

### 13. 离线地图瓦片缓存

PWA 已有 Service Worker，可以扩展缓存 Leaflet 瓦片，支持离线查看已浏览区域。

---

### 14. 3D 地形预览

节点数据有海拔，可以在景点详情页加一个迷你地形剖面图（SVG 折线），或用 `mapbox-gl` 做 3D 地形（但会引入重依赖，谨慎）。

---

### 15. 游记时间线的"那年今日"

`review.html` 日历已有基础，可以加：打开 App 时如果历史上的今天有游记，弹出一张卡片「去年今天，你在 ___」。

---

## 推荐落地顺序（Top 3）

如果只能选三个先落地：

1. **全局底部导航** — 解决 14 个页面的导航混乱，是所有其他体验提升的基础
2. **数据导出/备份** — 纯本地应用的生命线，用户敢用的前提
3. **行程规划真实路径** — 核心功能从"玩具"变"工具"的关键一步
