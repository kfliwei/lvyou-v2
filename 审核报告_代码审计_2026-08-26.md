# 行迹 TRACE 代码审计报告

> 审计日期:2026-08-26
> 审计方式:仅读代码(`.js/.html/.css/.java/.gradle/.webmanifest`),未参考任何 MD/文档。
> 方法:6 路并行深度审计(安全 / 核心设施 / 业务层 / 页面层 / 数据层 / Android+仓库),所有高危结论均经逐行或字节级二次验证。

## 项目概况

无构建步骤的原生 JS PWA(旅行足迹 + 行程规划 + AI 游记)+ WebView 安卓壳。

规模:HTML 约 428 KB(17 页)、核心 JS 约 800 KB、**数据 JS 约 4.6 MB**(含 1.45 MB 的 `nation-index.js`,7,851 条记录)、CSS 148 KB、图片约 12 MB;`android_app/` 磁盘占用 523 MB。

## 总评

**功能丰富、防御性解析习惯优秀的小型单人项目,但已撞上"靠人肉纪律同步"的架构天花板。**

质量与文件的新旧/大小成反比:`wishlist/vault/poster/node-lod` 等小模块堪称范本(手写 ZIP+CRC32 正确、IDB 迁移框架完整);而 `travel-notes.js`(146 KB,4 个模块拼贴、BOM 泄露拼接痕迹)、`node-manager.html`(983 行内联 JS)承载了绝大部分缺陷。更关键的是:**每一处"双份手工维护"都已产出至少一个线上 bug**(见下)。全库 0 个 TODO/FIXME/console.log 残留,git 包体 17.87 MB、密钥从未进过历史——底层工程素养是好的。

## 高危问题(全部已验证)

### 1. 存储型 XSS:导入笔记 JSON → 相册属性注入

`travel-notes.js:1823` 导入仅校验 `x.id`,整对象入库;`album.js:336-341` 将 `ch.photos[0]` 裸拼进 `src="..."`。构造 `"photos":["\" onerror=\"alert(1)\" x=\""]` 即可在打开相册时执行脚本。在 APK 内被放大:`MainActivity.java:53-54` 开启 `setAllowFileAccessFromFileURLs(true)` + `:128` 暴露 `AndroidVoice` 桥,XSS 可读本地文件、调原生方法。

*修复*:导入做字段白名单(photo 限 `data:image/`、`https?://`);所有属性位插值走 `UI.esc()`。

### 2. 每个 release APK 随包携带 90 MB 调试垃圾

`android_app/app/src/main/assets/img-test/` = 90 MB / 1,365 个文件,内含 Chromium 用户资料转储(`BrowserMetrics-spare.pma`、`Crashpad/settings.dat`)。APK 因此虚胖近一倍。**删掉即可,零成本收益最大的一项。**

### 3. Web 资产与 APK 双向漂移

APK 内打包的网页资产落后一代:壳内 `sw.js` 是 `trace-v22`、根目录已是 `v23`;含 9 个根目录已删的孤儿页,缺 `topic-catalog.js`;21/90 个共享文件校验和不一致。根因:assets 整目录被 gitignore(`android_app/.gitignore:2`),漂移对 git 不可见,同步全靠手动跑 `tools/sync-assets.js`。

### 4. 静默数据丢失:localStorage 无淘汰 + 配额错误被吞

`tn_rt_`/`tn_d_` 路线缓存(`planner.js:699-738`)无 TTL 无上限;所有关键写入 `tn_trips`/`tn_wishlist` 都是裸 `try{}catch(e){}`——配额一旦写满,用户存行程**无任何提示地丢失**。

### 5. 部署后首屏新旧资源混用

`sw.js:151-158` 静态资源 stale-while-revalidate(`return hit || f`),而全库 HTML 引用的都是无哈希文件名——发版后首次访问=新 HTML+旧 JS/CSS。唯一失效机制是手改 `trace-v23`(`sw.js:2`),无人强制。同页查询串导航也被永久入缓存(`sw.js:143`,`topic.html?p=sx`、`search.html?q=…` 各占一份)。

### 6. 数据四重编码,100% 冗余

`nation-index.js`(管道分隔字符串)与 33 个省份数据文件经程序化比对 **7,167/7,167 条完全重复**;`topic-meta.js`(119 KB 美化版)与 `topic-meta-lite.js`(56 KB 压缩版)是同一份注册表的两份手抄。改一条描述要同步四处,已经出现分叉。

### 7. `settings.html` 文档畸形

文件以 `</script><script></body></html>` 结尾(字节级确认)——尾部悬空 `<script>` 把 `</body></html>` 吞成 JS 源码(SyntaxError)。页面能渲染纯属浏览器容错。

### 8. 规划器加载错误省份数据

`planner.js:91` 映射 `江西 → changzheng-data.js`(长征专题数据!),`topic-common.js:355` 才是对的 `jx-data.js`。规划器里江西站点取不到 `best/elev/desc`。

## 中危问题(择要)

| 问题 | 位置 | 说明 |
|---|---|---|
| `javascript:` 链接穿透 Markdown 渲染 | `md-manager.html:311` | 图片分支做了协议白名单(`:308`),链接分支没做——同一策略两处实现只对了一处 |
| 用户自建节点名裸拼 innerHTML | `topic-common.js:562,772` | `wishlist.js:86` 注释明确写着"label 用户可控,走 textContent",策略执行不一致 |
| 内联 onclick 的 JS 字符串注入模式 | `planner.js:531,385` | `esc()` 转 `'`→`&#39;` 在 JS 字符串上下文会被解码还原,防不住;目前仅 self-XSS,但该模式遍布全库 |
| WebView 桥对任意跳转页面开放 | `MainActivity.java:81-93,97-99` | 外部链接在 WebView 内打开即可拿桥+被自动授予的地理定位;无 URL 白名单 |
| 行程数据完整性 | `planner.js:889-895,1063` | 保存两次产生分身副本;`t.days.reduce` 无形状校验,一条脏数据瘫痪整个已存列表 |
| 双页签互踩 | 全业务层 | 0 个 `storage` 事件监听,PWA+浏览器同时开=last-writer-wins 丢写 |
| 安全区 CSS 失效 | 8 个页面 | 用了 `env(safe-area-inset-*)` 却缺 `viewport-fit=cover`(仅 3 页有),刘海屏顶栏可能钻进刘海 |
| 字面量 `'undefined'` 当颜色值 | `topic-meta.js:647,658` | 陕西主题"溶洞奇观"配色就是字符串 "undefined" |
| 定位失败恢复流程死代码 | `index.html:338-340` | 该页唯一漏引 `ui.js` 的页面,`UI.confirm` 守卫静默跳过,"去地图选点"永远点不到 |
| 足迹 tab 有去无回 | `nav.js:8` | `travel-map.html` 既无 `#bnav` 也无 nav.js,底部导航落点是素页 |
| 国家索引坐标精度差 | `nation-index.js` | 34% 记录小数不足 2 位,281 条纯整数(误差可达百公里)——恰好是人不会重新生成的那份拷贝 |

## 低危/卫生问题(摘录)

- `sw.js:126` 瓦片域名子串匹配过宽;`sw.js:105` 预缓存失败静默吞掉
- `themeColor()` 配色表两页分叉(`wishlist.html:84` vs `review.html:310`)
- toast 实现 ×6(UI.toast 已存在仍各写各的);nation-index 解析逻辑复制 ×5
- 最近邻算法 ×5 份副本;`poster.js` "carto contour" 块原样粘贴两次
- 死 CSS:`design.css:921` `.rz-title`、`:1160` `.theme-ic`
- `node-manager.html:103` `'tn_user'+'Nodes'` 混淆式字符串拼接
- 根目录 0 字节文件 `=`(已被 ignore 但未删);6 张 `_*.png` 调试截图删除待提交
- iFlytek APPID 明文硬编码(`XfVoiceEngine.java:25`,SDK 公开标识,低危)

## 做得好的地方(应保持)

- **防御性解析近乎满分**:38 处 `JSON.parse(localStorage…)` 全部有 try/catch;损坏数据一律降级为空而非崩溃
- **真实的 IDB 迁移框架**(`travel-notes.js:87-103`)+ 缺 store 自愈(`album.js:39-53`)
- **AI Key 缺失全程优雅降级**(本地叙事模板兜底),用户永不会走进死胡同
- **分级懒加载有真实设计**:按 topic 动态注入 `<script>`、`nation-index` 三处按需加载
- **manifest 全链路核对通过**:图标存在、`start_url` 有效、颜色与 `design.css` 变量逐字节一致
- 日期处理统一规避 `new Date('YYYY-MM-DD')` 的 UTC 陷阱;唯一外链带 `rel="noopener"`
- 密钥管理底线守住:`tn-key.js` 从未进 git 历史,签名密码走 gitignored properties

## 根因归纳

三个系统性模式解释了几乎所有发现:

1. **同步靠纪律,不靠工具**——PROV_FILE 地图×2(已分叉)、themeColor 表×2(已分叉)、toast×6、pipe 解析×5、meta 注册表×2。每处重复都至少产出一个活 bug。
2. **转义策略在"上下文边界"失守**——`UI.esc()` 本身很好,但 HTML 属性位、JS 字符串位、URL 协议位这三类边界是全部 XSS 类问题的所在。
3. **发布链路三重手动**——手改缓存版本号 + 无哈希文件名 + 手动 assets 同步,三个独立的老化机制且全都触发过。

## 修复优先级建议

1. **今天就能做**:删 `img-test/`(-90 MB);修 `settings.html` 尾标签;修江西映射;补 `index.html` 的 `ui.js`
2. **本周**:XSS 三处(album 属性注入 / 导入白名单 / md 链接协议);SW 版本号改为构建脚本从 git hash 自动盖章;加一步 assets 校验和比对
3. **本月**:统一存储封装(TTL 淘汰+配额上报+storage 事件);抽公共 util 模块(esc/toast/haversine/uid 各 3-8 份副本);沿已有接缝拆分 `travel-notes.js`
4. **方向性**:单一权威 JSON 数据源 + 构建期派生(紧凑索引/lite 注册表),替代四套手抄编码——这是解开问题 6 和一半中危问题的钥匙

## 一句话结论

这是一个"小模块见功力、大文件露疲态"的项目——产品完成度和防御性习惯远超典型个人项目,但当下一笔功能债应该先还给数据同步机制和发布链路,而不是继续堆在新功能上。
